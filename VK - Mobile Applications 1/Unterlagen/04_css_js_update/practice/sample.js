BORDER: 5,   //the border on all four sides of the canvas.ROWS: 6,     //the number of rows on our boardCOLUMNS: 7,  //the number of columns on our boarddrawCell : function(canvas, x, y, state) {	var cellWidth = ((canvas.width - 2 * this.BORDER) / this.COLUMNS);	var cellHeight =  ((canvas.height - 2 * this.BORDER) / this.ROWS) ;		var ctx = canvas.getContext('2d');	ctx.lineWidth = 4;	ctx.strokeStyle = "#086788";	ctx.strokeRect (this.BORDER + x*cellWidth, 
					this.BORDER + y*cellHeight, 					cellWidth, cellHeight);	if (state == 1 || state == 2) {		if (state == 1) {			ctx.fillStyle = "#DD1C1A"; //red	  	}	  	else {			ctx.fillStyle = "#f6f600"; //yellow	  	}		var path = new Path2D();		path.arc(...);		ctx.fill(path);	}},